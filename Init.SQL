CREATE TABLE detail(
  ID INTEGER PRIMARY KEY AUTOINCREMENT NOT NULL ,
  DstIP VARCHAR(48) NOT NULL ,
  InsertTime NOT NULL DEFAULT CURRENT_TIMESTAMP,
  Delay FLOAT NOT NULL,
  Timeout TINYINT NOT NULL DEFAULT 0
);

